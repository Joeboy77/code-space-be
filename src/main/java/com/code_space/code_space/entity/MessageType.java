package com.code_space.code_space.entity;

public enum MessageType {
    MESSAGE,        // Regular chat message
    SYSTEM,         // System notification message
    ANNOUNCEMENT,   // Host announcement
    FILE,           // File attachment
    REACTION        // Reaction to a message (for future enhancement)
}