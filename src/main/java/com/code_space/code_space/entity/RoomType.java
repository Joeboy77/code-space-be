package com.code_space.code_space.entity;

public enum RoomType {
    INSTANT,        // Start meeting immediately
    SCHEDULED,      // Meeting scheduled for specific time
    PERSONAL        // Personal meeting room (like Zoom PMI)
}